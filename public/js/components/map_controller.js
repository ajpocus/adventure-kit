let React = require('react');

let MapController = React.createClass({
  render: function () {
    return (
      <div id="map">
        <h1>Map</h1>
      </div>
    );
  }
});

export default MapController;
