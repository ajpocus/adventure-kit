let React = require('react');

let MusicController = React.createClass({
  render: function () {
    return (
      <div id="music">
        <h1>Music</h1>
      </div>
    );
  }
});

export default MusicController;
